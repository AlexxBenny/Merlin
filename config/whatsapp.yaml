whatsapp:
  enabled: false                    # Opt-in. Default off.
  session_name: "merlin_whatsapp"
  database_path: "state/whatsapp/neonize.db"
  messages_dir: "state/whatsapp/messages"
  rate_limit:
    max_messages: 10
    window_seconds: 60
